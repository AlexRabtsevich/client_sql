import { makeStyles } from '@material-ui/core/styles';

export const getLoginStyles = makeStyles({
  root: {
    margin: '5rem 15rem',
    padding: '3rem',
    boxShadow: '0px 0px 36px rgba(74, 92, 136, 0.1), inset 0px 0px 4px rgba(74, 92, 136, 0.1)',
  },

});
